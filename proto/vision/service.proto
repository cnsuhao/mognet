syntax = "proto3";

package vision;

import "proto/vision/result.proto";

service Ocr {
    rpc Recognize(Frame) returns (OcrResult) {}
}

message Frame {
    bytes image = 1;
    int32 width = 2;
    int32 height = 3;
}
